Rewrote assembly code from scratch to be more efficient
Python code is template for our ISA. Pulled from last project since it follows the same Format(ISA is similar).
Program one only works for static Q = 17
